def test_check_date(xml):
    output = {}
    output = {
            'test':'check_date',
            'result' : '1'
             }
    return output

if __name__ == "__main__":
    app.run(debug=True)
